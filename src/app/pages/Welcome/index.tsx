import { styled, Box } from "@mui/material";

const WelcomePageContainer = styled(Box)(({ theme }) => ({
  display: "flex",

}));

const Welcome = () => {
  return <WelcomePageContainer>
    
  </WelcomePageContainer>;
};

export default Welcome;
